{
  "name": "announcements-yelbolt-worker",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "start:8888": "wrangler dev --port 8888",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "wrangler": "^3.90.0"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15"
  }
}
